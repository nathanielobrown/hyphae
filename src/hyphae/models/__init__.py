"""The types that cross package lines: the trace model every extractor builds and sink writes,
and the enrichment types a pass, the store and a page share — the vocabulary, the items it
describes and the row it writes."""
