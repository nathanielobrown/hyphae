"""The types that cross package lines: the trace model every extractor builds and sink writes,
the enrichment types a pass, the store and a page share — the vocabulary, the items it
describes and the row it writes — and what a store read hands back: the citation beside
every answer, a node's values, the rows each repository builds for the page that reads them,
and the answer `hp query` prints whole."""
