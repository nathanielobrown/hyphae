# Testing plan: viewer polish — one naming system, richer popovers, a readable reading pane

Obligations for `plans/viewer-polish/design.md`, grouped by that design's slices so each is due with its code. Every leaf is an obligation; the *Evidence:* clause names the artifact that discharges it, and an auditor traces each leaf to that artifact. Where a leaf names an existing test, the obligation is to extend or edit that test — a rule that changed while its test did not is a rule nothing checks.

Three rules shape everything below.

- **The seam is served bytes.** Assertions run through `TestClient` over the fixture corpus and read values out of `data-` attributes, never out of rendered prose and never out of the store the page read (`tests/view/conftest.py` docstring). The exceptions are the two pure modules — the formatter registry and the inline-markdown renderer — which test as functions
- **A recorded session beats an invented record, but redaction flattened the strings.** Every fixture README redacts the strings under a tool `input`, and no fixture records a model's enrichment, so markup and long values are planted onto real rows with `plant` / `enriched_plant` (`tests/view/conftest.py:planter`). A leaf that plants says so
- **What only a browser can show is driven or eyeballed, not asserted.** Popover fetch and placement belong to `tests/e2e/specs/htmx.spec.ts` (`mise run e2e`); colour, border weight and badge shape are witnessed on `mise run gallery` and dated in `.claude/rules/viewer-ui.md`

## Levels

- **unit (naming)** — `tests/view/test_formatters.py`. A tool name and the `tool_fields` column set in, `Formatted(mark, words)` out. No app, no store
- **unit (inline markdown)** — `tests/view/test_render.py`, beside the block renderer's safety pins, since the two share every escaping question
- **served HTML (NavTree)** — `tests/view/test_nav_tree*.py` over the session-scoped `client` fixture; rows read by `rows`/`fields`/`inside`/`values`
- **served HTML (node page)** — `tests/view/test_node*.py`, `test_walk.py`, `test_errors.py`, `test_run.py`
- **served HTML (fragments)** — `tests/view/test_numbers.py` and `tests/view/test_numbers__spend.py` over the `/fragment/numbers/…` routes, expectations priced from `live_api_calls` in the test's own SQL as those files already do
- **bounds and budgets** — `tests/view/test_bounds*.py` against `tests/view/budgets.py` and `src/hyphae/view/bounds.py`
- **fixture evidence (extract tier)** — a redacted excerpt proves the fields the viewer's queries then read
- **browser tier** — `tests/e2e/specs/htmx.spec.ts` under `mise run e2e`
- **eyeballed** — `mise run gallery` on `tests/view/scenarios.py`, witnessed and dated in `.claude/rules/viewer-ui.md`

---

## Slice 0 — schema and fixture evidence, due before slices 2 and 5

- **The design's open question is answered: `ToolSearch.query` and `PushNotification.message` are the recorded field names.** Read from the canonical store on 2026-08-28, read-only: 1,030 `ToolSearch` and 14 `PushNotification` tool calls. `ToolSearch.input` carries `query` and `max_results`; `PushNotification.input` carries `message` and `status`. *Evidence:* session `4208c1bd-78a0-46ef-9d3c-269b9b7a8e2b`, **Claude Code 2.1.221** — which is `tests/fixtures/spine`'s own source recording — holds five `ToolSearch` calls and one `PushNotification` on `main`, among them `toolu_01CcyHEsu4XugVyeSfS3U8hT` and `toolu_018UqB22UZkc6yAikcDQZryq`. No record model gains a field, so no `evidence.py` citation is due; the two names go into `analyze/macros.py:tool_fields` (`query` is already there) and into the registry's docstring with that session and version beside them.
- **`tests/fixtures/spine` is re-cut to carry one `ToolSearch` and one `PushNotification` call.** No fixture in the corpus records either tool (`grep -r '"ToolSearch"' tests/fixtures` is empty), so slice 2's surfaces have nothing to name. The cut comes from spine's own recording — same session, same version — so the corpus gains no session and its date span (2026-06-30 to 2026-08-06, spine's own day the latest) does not move; a fixture from a session outside that span would shift the window every analyze constant is measured against. *Evidence:* the amended README names the two source lines and the redaction call on the `PushNotification` message, which is prose an agent wrote about a private repo and must be replaced rather than trimmed; a store built from the fixtures answers `SELECT name, count(*) FROM tool_calls WHERE name IN ('ToolSearch','PushNotification') GROUP BY 1` with a row for each.
- The compaction evidence slices 5 needs is already cut and committed. `tests/fixtures/compaction` holds two main-thread boundaries with the api calls either side and the run `a003de2a5c1985f71` whose own thread compacted (`tests/conftest.py:COMPACTED`, `COMPACTED_BOUNDARY`, `COMPACTED_RUN`). *Evidence:* commit `f998863`, and the README's table of what each record carries; `compactMetadata`'s `trigger`, `preTokens` and `postTokens` are declared on `extract/records/shapes.py:CompactMetadata` and printed by `docs/schema.md`. No new cut is due.
- Markdown in a title needs no fixture at all. Redaction flattens every recorded string, so no fixture carries `**bold**` or a link, and `planter` exists for exactly this (`tests/view/conftest.py:139`). *Evidence:* slice 3's leaves plant the markup onto a real enrichment row through `enriched_plant` and say so.

## Slice 1 — naming core

- **One name for a tool call, composed in Python, on every surface that prints one.** *Evidence:* `test_one_tool_call_is_titled_the_same_way_wherever_it_is_named` (`tests/view/test_node__titles.py`) and `test_every_registered_tool_the_corpus_records_agrees_across_its_surfaces` (same file) pass with the api-call title and the calls-log column added to the surfaces they compare — the same asserted string in the NavTree row, the crumb chain, the pane heading, the parent's log row, and the api-call row above it. Bolded: one string across five surfaces is the whole point of the consolidation, and it is asserted nowhere else.
- An api-call NavTree row leads with the glyph of the tool it called. *Evidence:* `test_an_api_call_that_answered_with_tool_calls_is_named_by_what_it_called` (`test_node__titles.py`) extended — the row's title begins with the mark `formatters.name_tool` returns for that call's first tool, read off the store rather than spelled here.
- **A tool the registry does not name keeps the exact title the `tool_title` macro produced.** *Evidence:* a sweep over every tool row of every fixture session asserting the served title equals the shape-driven fallback for that row's `input` — path, else description, else the head of the stored JSON — computed in the test's own SQL from the old macro body, which the diff moves into the test as the expectation before deleting it from `macros.py`. Bolded: the fallback is the arm no registry entry covers and the one a port silently changes. See the seam finding below: the fallback needs the raw input head, which `tool_fields` does not carry today.
- Paths in a name stay relativized against the session's project. *Evidence:* the `Read` and `Edit` rows on a mycelia fixture session read `src/…` with no `/Users/nob/repos/mycelia` prefix, checked against `sessions.project` — carried by the surfaces sweep above rather than by a leaf of its own.
- The count of a call's tools survives every width the title is cut to. *Evidence:* `test_the_count_of_a_calls_tools_survives_every_width_the_title_is_cut_to` (`test_node__titles.py`) passes with the aggregation moved out of SQL.
- **A query that ships fields instead of a title stays bounded.** *Evidence:* `test_no_page_or_fragment_query_selects_a_fat_column_whole` and `test_every_macro_the_scan_trusts_cuts_the_value_it_reads` (`tests/view/test_bounds.py:233`, `:149`) green over the changed `view_nav_tree_calls`, `view_call_header`, `view_turn_calls` and `view_numbers_tool`, with `BOUNDING` in `macros.py` naming only macros that still exist. Bolded: per-row fields where there used to be one aggregated string is the change most able to multiply a page's bytes, and the scan is what sees it.

## Slice 2 — naming everywhere

- **The calls-log column and the popover's sibling list name their tools the way the NavTree does.** *Evidence:* on the re-cut `spine`, the turn's calls-log row and the `/fragment/numbers/…/tool/…` sibling list both carry the `ToolSearch` mark and the recorded `query`; the same assertion runs through `test_a_call_row_says_what_the_call_said_and_which_tools_it_called` (`test_node__rows.py:156`) and `test_a_tool_call_says_what_it_gave_back_and_what_was_asked_beside_it` (`test_numbers.py:389`). Bolded: these two surfaces are where the emoji regression showed, and the design's whole first problem is that nothing asserted them together.
- `ToolSearch` names its call by `query` and `PushNotification` by `message`. *Evidence:* two rows in `tests/view/test_formatters.py`'s table, their inputs lifted from session `4208c1bd` (slice 0) rather than invented, plus the served-HTML reading above — so the registry entry is proved against a recording and against a page.
- Every registered tool the corpus records still agrees across its surfaces once two more tools are in the corpus. *Evidence:* `test_every_registered_tool_the_corpus_records_agrees_across_its_surfaces` (`test_node__titles.py`) passes unchanged over the re-cut fixture — the sweep reads the registry, so the two new tools enter it without the test naming them.
- Deleting the naming macros leaves no query selecting one. *Evidence:* `grep -r 'tool_title\|tool_about' src/hyphae/analyze` is empty, and `test_every_macro_the_scan_trusts_cuts_the_value_it_reads` (`test_bounds.py:149`) passes with `BOUNDING` shrunk to the macros that remain.
- The calls-log sub-line survives the loss of `tool_about`, or its loss is deliberate. *Evidence:* `test_the_two_prose_columns_of_a_calls_log_are_bounded_by_the_stylesheet` (`test_node__rows.py:274`) asserts what a `Bash` log row shows under its title after the change — the description composed in Python, or the line gone by decision recorded in the design.

## Slice 3 — inline markdown

- **Bold, italic and code render in a title; a link renders as an anchor only outside an existing link.** *Evidence:* one enrichment description planted through `enriched_plant` carrying `**bold**`, `` `code` `` and `[PR #18](https://github.com/…)`, read on four surfaces of the same node: the reading-pane `<h1>` carries a `<strong>` and an `<a href>`; the NavTree row, the crumb chain and the walk control carry the `<strong>` and the link's text with no nested `<a>`. Bolded: link-inside-a-link is the failure the design's rule exists to prevent, and only reading the same planted string on both kinds of surface catches it.
- The `<title>` element and every attribute carry plain text. *Evidence:* the same planted page's `<title>` holds neither `**` nor `<`, and no `data-` attribute on the page contains a tag.
- **The inline renderer passes the block renderer's safety pins.** *Evidence:* leaves beside the existing ones in `tests/view/test_render.py` — raw HTML in the source arrives as text (`:13`), an inline `onerror` attribute cannot ride in (`:28`), image syntax fetches nothing (`:35`), and only an `http` URL becomes a link (`:59`) — run against `inline_markdown.render`. Bolded: the new module is a second escape path into every page, and `test_app__safety.py`'s CSP leaf does not see string composition.
- No block element escapes into a title. *Evidence:* a planted description holding a heading, a list and a fenced block renders to text with no `<h`, `<ul>`, `<p>` or `<pre>` in the NavTree row.
- Width cuts measure the plain text. *Evidence:* `test_a_crumb_is_cut_narrower_than_every_other_place_a_title_is_read` (`test_node.py:456`) extended with a planted title whose markdown syntax exceeds the crumb cut — the visible characters count to the same length as the same title without syntax, so the asterisks do not eat the budget.
- A title with no markdown in it is byte-identical to what it was. *Evidence:* over every fixture session, every NavTree row title on the unplanted corpus matches the value the store holds, so the renderer is a no-op on flat text.
- **A title the query cut is marked, whatever the markdown it was written in renders to.** *Evidence:* two readings of the same rule in `tests/view/test_render.py` — `cut` marks a raw string longer than the `source_cap` it is handed even where the visible budget was never spent, and leaves a complete string whose syntax outruns a narrow surface unmarked. The measured regression is the pin: 280 characters of `**ab** ` arrive from the NavTree's query as 111, render to 47 visible characters, and printed with nothing saying the rest was dropped (`handoffs/impl-slice3-markdown.md`). Bolded: an unmarked cut is a page saying a session said this much and no more, which is the one thing a title may not get wrong.
- **A markdown run the query cut in half is dropped, not printed as the delimiters it now ends in.** *Evidence:* a leaf in `test_render.py` — `**ab** **cd` under a cap of 10 serves the bold run and nothing of the broken one, because nothing at print time can recover the closing `**` the store never shipped.
- The cap a cut is measured against is the one the query behind the words used. *Evidence:* a served reading in `tests/view/test_node__markdown.py` — a planted prompt of `**ab** ` past the NavTree's width comes back marked in the row while the row's own budget went unspent, and a shorter run of the same markup planted as a *description*, which `view_enrichment` cuts at a width of its own, comes back unmarked.

## Slice 4 — popover breakout

- **The session popover's base lines become own-thread, and the change is asserted rather than inherited.** *Evidence:* `test_a_session_reads_its_window_off_the_main_thread_and_its_dollars_off_them_all` (`test_numbers.py:156`) rewritten: the three dollar figures price only `source = 'main'` calls in the test's own SQL, and on a session with subagents they are strictly less than `session_rollups.cost_usd`. Bolded: this is the one semantic reversal in the design, and the existing test passes today on the opposite rule.
- **A node's total-spend line equals what its own NavTree badge already prints.** *Evidence:* on `spine`, for the session row and for a turn that spawned a run, the popover's total equals the `total` half of the dual badge for the same key — the expectation computed once, through the `SPAWNS` SQL in `tests/view/conftest.py` that `test_a_dual_badge_gathers_under_a_row_every_run_it_spawned` (`test_nav_tree__badges.py:94`) already uses. Bolded: two surfaces are now drawing the same rollup from different queries, and only comparing them catches a divergence.
- Own plus subagent spend equals total, on every node of every fixture session that has both. *Evidence:* a sweep over the `/fragment/numbers/…` routes reachable from `pages()` asserting the arithmetic to cent precision, beside the existing column-sum leaf `test_the_popovers_two_columns_come_to_the_totals_under_them` (`test_numbers.py:252`).
- **A node with no subagent spend under it shows no breakout at all.** *Evidence:* the popover for a run on `MODEL_ONLY`'s session, and for a `call:` node on `spine` whose api call spawned nothing, carries neither the subagent-spend nor the total-spend field — asserted by the absence of the two `data-field` names, not by a string search. Bolded: the design's rule is conditional emission, and a template that always renders the lines would pass every leaf above.
- Each breakout dollar takes the badge's own wash. *Evidence:* `test_every_dollar_in_a_popover_is_washed_at_its_share_of_what_the_session_spent` (`test_numbers__spend.py`) extended to the two new fields, comparing against `nodes.meter` by name.
- Every popover route still answers 200 and stays inside its budget. *Evidence:* the `pages()` route sweep and `test_a_served_page_stays_under_its_ceiling` (`test_bounds.py:446`) with the fragment budget in `tests/view/budgets.py` re-derived, its comment naming the two lines that added the bytes.

## Slice 5 — compactions

- **A run row whose thread compacted carries the badge and its count; a run that did not carries none.** *Evidence:* on `tests/fixtures/compaction`, the row for `COMPACTED_RUN` carries the badge `data-` attribute with the count `view_runs.sql` holds, and `SPINE_RUN` carries no such attribute — a new leaf in `tests/view/test_nav_tree__rows.py`. Bolded: the corpus has exactly one run in this shape, so this leaf is the only reading of the feature the suite can take.
- **A compaction row serves its numbers.** *Evidence:* `test_a_kind_with_no_numbers_is_a_route_that_answers_nothing` (`test_numbers.py:614`) edited — compaction leaves that list and buckets stay in it — and a new leaf reading the compaction fragment for `COMPACTED_BOUNDARY`: context before, context after, freed, and the recorded `trigger`, each against `live_compactions` in the test's own SQL. Bolded: the existing test asserts today's rule, so a design that reverses it and leaves that test green has reversed nothing.
- The compaction row fetches its popover the way every other row does. *Evidence:* the `⊟` row in the NavTree carries an `hx-get` to the compaction fragment route, matched against the route the app mounts rather than a literal path.
- `tests/view/scenarios.py` gains the compaction numbers fragment, with a title and a group. *Evidence:* `test_every_route_the_viewer_exposes_is_in_the_payload_sweep` (`test_bounds.py:470`) and both leaves of `test_scenarios.py` green; the citation sweep `test_every_citation_a_page_carries_links_to_the_query_it_names` (`test_query.py:74`) covers the new SQL file.
- **`bounds.NAV_TREE_ROW_BYTES` is re-measured against the widest served row, not raised until green.** *Evidence:* the measurement in `tests/view/test_bounds__node.py` reports the widest row's byte count and the constant equals it with its "no slack" comment intact; the diff shows the new number beside the old (1929). The badge and the markdown spans of slice 3 both land in this number, so it is measured once, here. Bolded: the constant is the only thing standing between a per-row addition and a NavTree that costs a page. *Superseded:* the Phase 2 split (PR #28) moved that measurement into `weighed()`, which the three sweep leaves share.
- The page budgets in `tests/view/budgets.py` are re-derived rather than relaxed. *Evidence:* each changed constant's comment names what added the bytes; `test_a_served_page_stays_under_its_ceiling` passes without a blanket increase.

## Slice 6 — reading pane

- **A tool call's Arguments are shown as highlighted JSON, in the pane and in the fetched rest alike.** *Evidence:* `test_a_pane_previews_a_fat_value_and_offers_the_rest_as_its_own_fetch` (`test_node__details.py:34`) extended — the `input` detail carries the highlighter's JSON class in the pane, and `test_every_value_a_pane_previews_is_fetchable_whole_from_its_own_url` (`:418`) shows the fetched fragment carrying the same classes, so the two paths cannot diverge again. Bolded: one rule serving two paths is the design's fix, and the divergence is exactly what shipped.
- A Result with a known suffix keeps its suffix syntax; a Result that is JSON is highlighted as JSON; anything else renders plain and unbroken. *Evidence:* three readings — `test_a_read_of_a_markdown_file_shows_the_source_marked_up_and_not_rendered` (`test_node__details.py:351`) unchanged for the suffix arm, a recorded tool result that parses as JSON carrying JSON classes, and a plain-text result carrying no highlight class but its whole text; the third arm is what proves the parse-then-fall-back path does not swallow content.
- The highlighter's own contract is unchanged. *Evidence:* `tests/view/test_highlight.py` and `test_the_sheet_paints_only_classes_the_highlighter_can_emit` (`test_query.py:217`) pass with no edit.
- Prose details carry the quote border and payload details do not. *Evidence:* on a run page and an api-call page, the prompt, brief, said, thought and run-result details carry the border class and the tool `input`/`result` details on a tool page do not — a class-presence reading over both pages, since the border is a CSS hook the server emits.
- The session page drops its Title and Project fact rows and keeps everything else. *Evidence:* `test_every_kind_of_node_serves_a_page_that_says_what_it_is` (`test_node.py:79`) with the session's expected field set edited; the project stays reachable as the crumb `test_the_crumb_chain_leads_with_the_way_back_out_of_the_session` (`:407`) asserts, so the fact is dropped and not lost.
- A run page still shows its Task brief. *Evidence:* `test_a_run_page_reads_the_call_that_spawned_it_for_the_ask_and_the_answer` (`test_node__details.py:165`) and `test_a_run_nobody_asked_in_words_shows_no_ask_and_serves_none` (`:210`) pass unchanged — the design reverses the original ask, so the obligation is that these two tests do not move.
- **Popover fetch and placement still hold with a fourth kind and two more lines.** *Evidence:* `tests/e2e/specs/htmx.spec.ts:138` — "pointing at a row fetches its popover once and stands it at the row's top" — extended to point at a compaction row beside the session, turn and run rows it already visits, run under `mise run e2e` against a gallery whose store holds `tests/fixtures/compaction`. Bolded: the popover is fetched, positioned and cached by a script no served-HTML leaf can execute, and slice 5 adds a row kind to it.
- The quote border, the red badge and the JSON block read correctly in both colour schemes. *Evidence:* eyeballed on `mise run gallery` on a port that is not 8477, in light and dark, dated in `.claude/rules/viewer-ui.md` per the protocol that file already uses.

## Slice 7 — docs

- The display-vs-retrieval convention is written where an agent will meet it. *Evidence:* the sentence exists in the file `docs/documentation.md` places it in, and `AGENTS.md`'s pointer chain reaches it; `mise run check-fast` resolves the link.
- `CONTEXT.md` defines the terms this change coins and redefines the popover. *Evidence:* *compaction badge* and the popover's spend lines read against the code in the same PR; nothing is defined that the change did not coin.
- `docs/viewer.md` says what a popover now shows and what names a tool call. *Evidence:* the generated blocks pass `mise run check`'s freshness step, and `grep` finds no surviving claim that a tool call is titled by `analyze/macros.py`.
- `docs/schema.md` and the record models are untouched, deliberately. *Evidence:* no field was added to a record model — `ToolSearch.query` and `PushNotification.message` are read out of the stored `input`, which the store already keeps whole — so the schema document has nothing to gain; slice 0 records the confirming session instead.
- `doc-sync` runs before the PR. *Evidence:* the PR carries its report, per `docs/pull-requests.md`.

---

## Seam findings for the designer

- **The ported fallback needs a field the design's contract does not ship.** `name_tool(fields)` takes "the `tool_fields` column set", but the fallback's last arm is the head of the raw input JSON, and `tool_fields` carries no such member. Today the queries hand Python both `tool_fields` and `tool_title`, and `builders.tool_node` falls back to the latter (`view_nav_tree_tools.sql:22`, `builders.py:188`). Either `tool_fields` gains a bounded `head` member or the queries keep shipping a cut `input`; both stay inside the byte scan, but the design should say which. The slice-1 fallback leaf is unreachable until it does.
- **The design says `tool_asked` and `tool_path` are deleted, but `tool_fields` calls both.** `macros.py:_TOOL_FIELDS` is built out of them, and `tool_path` is what relativizes a path against the project. Read as a slip for "`tool_title` and `tool_about`"; confirm before the implementer deletes anything.
- **The formatter table has no served-HTML evidence beyond the tools the corpus records.** Every fixture README redacts the strings under a tool `input`, so a served row proves the registry fired, not what it read. `ToolSearch` and `PushNotification` escape this only because slice 0's cut keeps their arguments — which is why that cut's README has to state the redaction call on the `PushNotification` message. Widening the corpus's redaction rule is a decision about the whole fixture set, not about this change.

## Unreachable

Nothing in the design is unreachable through its seam once the two macro findings above are settled. The fallback leaf under slice 1 is the one obligation blocked on a design answer rather than on implementation.

## Not covered

- **Anything CSS computes.** Border weight and colour, badge shape, the depth of a wash, popover geometry beyond the top-alignment the browser tier already asserts. The server emits classes; the browser decides what they look like. Eyeballed on the gallery and dated in `.claude/rules/viewer-ui.md`
- **Markdown in detail values beyond what already renders as prose.** Out of scope in the design; the existing leaves in `test_node__details.py:93` are regression cover
- **The popover script itself.** No JS test runner in this repo. The obligation stops at what `mise run e2e` drives and at `test_the_popovers_placement_rides_a_file_the_policy_allows` (`test_numbers.py:335`)
- **Per-turn and per-session compaction badges.** Out of scope in the design; no query exists to feed them
- **The cost badge and the context bar.** Untouched by this change; their leaves in `test_nav_tree__badges.py` and `test_nav_tree__bars.py` are regression cover, not obligations of this change
